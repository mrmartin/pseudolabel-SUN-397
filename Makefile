extract:
	echo "extract last (1000) and second-last (4096) layers from a set of images"
	./extract_features.sh

todo:
	#extract last 1000 and second-last layers for SUN dataset images
	#run the same experiment as before, but with this new data (not 7 classes, but 397)
	#remove duplicates, which images whose output from the network is the same
	
